#version 330 core
out vec4 FragColor;
in vec3 vertexColor;
in vec2 TexCoord;

uniform sampler2D globalTexture;

void main()
{
	// FragColor = vertexColor;
	// FragColor = globalColor;
	// FragColor = vec4(1.0f);
	FragColor = texture(globalTexture, TexCoord) * vec4(vertexColor, 1.0);
}
